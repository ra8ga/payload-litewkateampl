# Test environment variables for local/CI runs
# Copy to .env.test and fill with suitable values

# General
CLOUDFLARE_ENV=dev
# Optional: override base host for smoke/E2E
SMOKE_BASE=
E2E_BASE_URL=

# Upload smoke (tests/smoke/upload.spec.ts)
SMOKE_EMAIL=
SMOKE_PASSWORD=